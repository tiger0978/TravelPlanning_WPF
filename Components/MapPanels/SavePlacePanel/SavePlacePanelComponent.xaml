<UserControl
    x:Class="TravelPlanning.Components.MapPanels.SavePlacePanel.SavePlacePanelComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TravelPlanning.Components.MapPanels.SavePlacePanel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:saveList="clr-namespace:TravelPlanning.Components.SaveList"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="#FAFAFA">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border
                Height="48"
                Margin="20,25,20,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Background="White"
                BorderBrush="#DEE2E6"
                BorderThickness="1"
                CornerRadius="24">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.2"
                        ShadowDepth="2" />
                </Border.Effect>
                <Grid Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="PlaceContainer" Grid.Column="0" />
                    <ui:SymbolIcon
                        Grid.Column="1"
                        Cursor="Hand"
                        Foreground="#70757A"
                        Symbol="Search24" />
                </Grid>
            </Border>
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            Margin="20"
            HorizontalAlignment="Center">
            <Button
                Width="360"
                Height="42"
                Padding="16,6"
                HorizontalAlignment="Center"
                Background="SteelBlue"
                BorderThickness="0"
                Command="{Binding AddListCommand}"
                Cursor="Hand"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="White">

                <Button.RenderTransform>
                    <ScaleTransform x:Name="scale" ScaleX="1" ScaleY="1" />
                </Button.RenderTransform>

                <Button.Effect>
                    <DropShadowEffect
                        BlurRadius="10"
                        Opacity="0.25"
                        ShadowDepth="2" />
                </Button.Effect>

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="20">
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">

                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    FontSize="16"
                                    Symbol="BookmarkAdd24" />

                                <TextBlock VerticalAlignment="Center" Text="新增清單" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--  Hover  -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="LightSteelBlue" />
                            </Trigger>
                            <!--  Click animation  -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
        <saveList:SaveListComponent Grid.Row="2" SelectedItem="{Binding DataContext.SelectedItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />

    </Grid>
</UserControl>
