<UserControl x:Class="TravelPlanning.Components.MapPanels.SavePlacePanel.SavePlacePanelComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TravelPlanning.Components.MapPanels.SavePlacePanel"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:saveList="clr-namespace:TravelPlanning.Components.SaveList"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FAFAFA">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Border VerticalAlignment="Top" 
                     HorizontalAlignment="Stretch"
                     Margin="20,25,20,0" 
                     Height="48" 
                     Background="White" 
                     CornerRadius="24"
                     BorderBrush="#DEE2E6" 
                     BorderThickness="1">   
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2"/>
                </Border.Effect>
                <Grid Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" x:Name="PlaceContainer"></Grid>
                    <ui:SymbolIcon Symbol="Search24" Grid.Column="1" Foreground="#70757A" Cursor="Hand"/>
                </Grid>
            </Border>
        </StackPanel>

        <saveList:SaveListComponent Grid.Row="1" SelectedItem="{Binding DataContext.SelectedItemCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" ></saveList:SaveListComponent>
        
    </Grid>
</UserControl>
