<UserControl
    x:Class="TravelPlanning.Components.SaveList.SaveListComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <ListView
            Background="White"
            BorderThickness="0"
            ItemsSource="{Binding SaveLists}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Cursor" Value="Hand" />

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F2F2F2" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>


            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="12,8" Background="Transparent">
                        <Grid.InputBindings>
                            <MouseBinding
                                Command="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Gesture="LeftClick" />
                        </Grid.InputBindings>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Icon  -->
                        <ui:SymbolIcon
                            Grid.Column="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="20"
                            Foreground="#1A73E8"
                            Symbol="{Binding IconKey}" />

                        <!--  Text  -->
                        <StackPanel Grid.Column="1" Margin="12,0,0,0">

                            <TextBlock
                                FontSize="14"
                                FontWeight="SemiBold"
                                Foreground="#202124"
                                Text="{Binding Name}"
                                TextTrimming="CharacterEllipsis" />

                            <TextBlock
                                Margin="0,2,0,0"
                                FontSize="12"
                                Foreground="#5F6368"
                                Text="{Binding Description}"
                                TextTrimming="CharacterEllipsis" />

                        </StackPanel>

                        <!--  More menu  -->
                        <Button
                            Grid.Column="2"
                            Width="35"
                            Height="35"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="MoreMenu_Click"
                            Cursor="Hand"
                            Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <Button.ContextMenu>
                                <ContextMenu
                                    x:Name="ItemMenu"
                                    Background="White"
                                    BorderThickness="1">
                                    <ContextMenu.Resources>
                                        <Style TargetType="MenuItem">
                                            <Setter Property="Padding" Value="10,8" />
                                            <Setter Property="Cursor" Value="Hand" />
                                            <Setter Property="Foreground" Value="#202124" />
                                            <Setter Property="Background" Value="white" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="MenuItem">
                                                        <Border
                                                            x:Name="Border"
                                                            Padding="{TemplateBinding Padding}"
                                                            Background="{TemplateBinding Background}">
                                                            <ContentPresenter Content="{TemplateBinding Header}" RecognizesAccessKey="True" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsHighlighted" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="LightGray" />
                                                                <Setter Property="Foreground" Value="White" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ContextMenu.Resources>
                                    <MenuItem
                                        Command="{Binding PlacementTarget.Tag.HideLayerCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding MapLayerId}"
                                        Foreground="#202124"
                                        Header="隱藏此清單" />
                                    <MenuItem
                                        Command="{Binding PlacementTarget.Tag.DeleteSaveListCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding MapLayerId}"
                                        Foreground="#202124"
                                        Header="刪除清單" />
                                </ContextMenu>
                            </Button.ContextMenu>

                            <ui:SymbolIcon
                                FontSize="16"
                                Foreground="#5F6368"
                                Symbol="MoreVertical24" />
                        </Button>

                    </Grid>


                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>