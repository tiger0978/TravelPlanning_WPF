<ui:FluentWindow x:Class="TravelPlanning.Views.MainTravelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:pages="clr-namespace:TravelPlanning.Views.Pages"
        xmlns:local="clr-namespace:TravelPlanning.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="MainTravelWindow" Height="550" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="建立旅遊計畫" Grid.Row="0"/>
        <ui:NavigationView 
            x:Name="navigationView"
            PaneDisplayMode="Left" 
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="True"
            Grid.Row="1"
            MenuItemsSource="{Binding MenuItems}">
          
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Setting" Icon="{ui:SymbolIcon Settings24}"  />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>
    </Grid>

   
</ui:FluentWindow>
